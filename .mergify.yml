pull_request_rules:
  - name: Automatic merge on approval and CI success
    conditions:
      - base=main
      - label="Please Merge"
      - status-success=build
      - "#review-requested=0"
    actions:
      merge:
        method: squash
